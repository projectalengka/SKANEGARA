# ---- Database (PostgreSQL / Supabase) -------------------------------------
# Pooled connection used by the application at runtime.
DATABASE_URL=
# Direct (non-pooled) connection used by `prisma migrate` and `prisma db push`.
DIRECT_URL=

# ---- Admin authentication --------------------------------------------------
# Any long random string, e.g. `openssl rand -base64 48`.
# Used to sign the admin session cookie. Minimum 32 characters.
AUTH_SECRET=
# Initial administrator account, created by `npm run db:seed`.
ADMIN_EMAIL=
ADMIN_PASSWORD=

# ---- Supabase (auth + storage) --------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# ---- Cloudinary (image storage) -------------------------------------------
# Empty = uploads disabled, but not silently: the dashboard shows
# "Unggah gambar belum aktif" in the sidebar and the upload field explains why.
# Setup: README.md section 5 / DEPLOY.md Bagian 7.
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# ---- Site ------------------------------------------------------------------
# No trailing slash. Used for canonical URLs, sitemap and Open Graph.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ---- Contoh data (opsional) ------------------------------------------------
# Mengisi halaman berita, kegiatan, galeri dan karya dengan data contoh
# bertanda `[CONTOH]` supaya tata letaknya bisa dinilai sebelum tulisan asli
# sekolah siap.
#
#   kosong / `off`  → situs menampilkan keadaan kosong yang sebenarnya (bawaan)
#   `on`            → data contoh ditampilkan ke pengunjung
#
# Hanya nilai `on`, `true`, atau `1` yang mengaktifkan. Nilai lain — termasuk
# salah tulis — dianggap mati, supaya tidak pernah menyala tanpa sengaja.
#
# Data contoh tidak pernah menimpa isi yang sudah Anda tulis sendiri: baris
# basis data selalu menang. Lihat `src/data/sample.ts`.
SAMPLE_DATA=
